// linked list with a tail pointer
// when we add to the queue, we can just put it at the tail

// when we dequeue, we just remove the head

// with stacks, they are implemented as an array, with popping and pushing
